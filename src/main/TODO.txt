Transaction Handling: Facilitate book checkouts and returns
track due dates
manage overdue fines.


add dueDate to transaction
set it during borrow
display it in ui/html

add dueDate overdue detection
show overdue in ui/html

add fine calculation

dueDate

dueDate = borrowDate + days

detect overdue

if (today > dueDate && !returned)

add fine

double fine;

$100 per day late
long daysLate = Days.between(dueDate, today)
fine = daysLate * 100;

add borrow limit

add time -
overdue time passed (displays warning "Due date passed, return book")
